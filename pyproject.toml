[project]
name = "python-project-template_test"
version = "0.1.0"
description = "A cookiecutter template for Python projects"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "pre-commit>=3.8.0",
    "ruff>=0.6.3",
]

[project.urls]
